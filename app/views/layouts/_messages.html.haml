.row
  .col-md-12
    - flash.each do |name, msg|
      %div{:class => "alert alert-#{ message_change( name ) } "}
        %a{:data-dismiss => "alert", :href => "#", :class => "close"} &#215
        = content_tag :div,  msg, :id => "flash_#{name}" if msg.is_a?(String)
